<%= form_for(venda) do |f| %>
  <% if venda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(venda.errors.count, "error") %> prohibited this venda from being saved:</h2>

      <ul>
      <% venda.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data %>
    <%= f.date_select :data %>
  </div>

  <div class="field">
    <%= f.label :nome_comprador %>
    <%= f.text_field :nome_comprador %>
  </div>

  <div class="field">
    <%= f.label :receita %>
    <%= f.text_field :receita %>
  </div>

  <div class="field">
    <%= f.label :focas %>

  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>